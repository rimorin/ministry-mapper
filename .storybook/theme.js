import { create } from "@storybook/theming/create";

export default create({
  base: "light",
  brandTitle: "Ministry Mapper",
  brandUrl: "https://ministry-mapper.com",
  brandImage: "https://ministry-mapper.com/logo.png",
  brandTarget: "_self"
});
